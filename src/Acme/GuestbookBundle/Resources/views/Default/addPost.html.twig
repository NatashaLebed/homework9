{% extends "AcmeGuestbookBundle::layout.html.twig" %}

{% block title "Welcome " %}

    {% block content %}

        <div id="wrap">
            <div class="container">
                <div class="alert alert-success">
                    {{ form_start(form) }}
                    {{ form_errors(form) }}

                    {{ form_row(form.User) }}
                    {{ form_row(form.Mail) }}
                    {{ form_row(form.Message) }}
                    {{ form_end(form) }}
                </div>
            </div>
        </div>

    {% endblock %}